"""Per-source response parsers → canonical tidy long.

One parser today (``cdss_climate``); the package layout mirrors the sibling
``streamflow`` pipeline so a second source/vintage is a new module here.
"""
